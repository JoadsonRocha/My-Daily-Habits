import HabitList from "../components/HabitList";

/**
 * Página de Lista de Hábitos HabitsPage (Apostila - Seção 9.3: Rotas Aninhadas)
 *
 * Renderizada no index do HabitsLayout (caminho "/habitos").
 * Exibe a lista completa de hábitos consumindo o contexto diretamente.
 */
export default function HabitsPage() {
  return <HabitList />;
}
